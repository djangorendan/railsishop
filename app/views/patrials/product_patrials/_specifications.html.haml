%ul
  - @product.properties.roots.each do |property|
    %li
      #{property.name} :
    %ul
      - property.children.order(:position).each do |child|
        - if child.product_properties.find_by(product: @product).value?
          %li
            #{child.name} :
            =child.product_properties.find_by(product: @product).value
