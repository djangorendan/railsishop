<% content_for :head do %>
  <title><%= @category.name %> | Angar.kh.ua</title>
<% end %>

<h1><%= @category.name %></h1>

<div class="d-xl-none d-lg-block"  style="margin-bottom: 10px;">
  <%= render "patrials/filter_form" %>
</div>

<select id="product_filter_order" class="form-control" style="margin-bottom: 10px;">
  <option value="views asc">  по популярности </option>
  <option value="price asc">  от дешевых к дорогим </option>
  <option value="price desc">  от дорогих к дешевым </option>
</select>

<div id="products_container">
  <%= render "patrials/products" %>
</div>

<div class="">
  <%= @category.discription.html_safe %>
</div>
