<%= form_for [:admin, product_photo] do |form| %>
  <% if product_photo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(product_photo.errors.count, "error") %> prohibited this product_photo from being saved:</h2>

      <ul>
      <% product_photo.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :image %>
    <%= form.file_field :image %>
  </div>

  <div class="field">
    <%= form.label :main %>
    <%= form.check_box :main %>
  </div>

  <div class="field">
    <%= form.text_field :product_id, value: (params[:product_id]), class: "d-none" %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
