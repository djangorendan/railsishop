<%= form_for [:admin, @app_image] do |form| %>
  <% if app_image.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(app_image.errors.count, "error") %> prohibited this app_image from being saved:</h2>

      <ul>
      <% app_image.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :image %>
    <%= form.file_field :image %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
